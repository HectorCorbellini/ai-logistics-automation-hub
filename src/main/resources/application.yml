spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
  mail:
    host: smtp.example.com
    port: 587
    username: ${EMAIL_USERNAME:hector.corbellini@gmail.com}
    password: ${EMAIL_PASSWORD:placita2010}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

slack:
  webhook:
    url: ${SLACK_WEBHOOK_URL:https://hooks.slack.com/services/your/webhook/url}

groq:
  api:
    key: ${GROQ_API_KEY:your_groq_api_key}
    url: https://api.groq.com/openai/v1/chat/completions
  model: llama-3.1-8b-instant
